api_platform:
    title: 'oembed-bookmark Api'
    version: 1.0.0
    show_webby: false
    # Mercure integration, remove if unwanted
    mercure:
        include_type: true
    # Good defaults for REST APIs
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
